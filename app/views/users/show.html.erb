<div class="container px-5">
    <div class="row">
        
          <%= render 'user', user: @user %>
          <div class="col-md-8 offset-1">
              <h2>Books</h2>
              <table class="table table-hover">
                  <tr>
                      <th></th>
                      <th>Title</th>
                      <th>Opinion</th>
                      <th colspan="3"></th>
                  </tr>
                  <%= @books.each do |book| %>
                      <tr>
                          <td><%= image_tag book.user.get_profile_image(100,100) %></td>
                          <td><span><%= link_to book.name, book_path(book.id), method: :get %></span></td>
                          <td><span><%= book.body %></span></td>
                          <td>
                              <a class="btn" href="/books/:id/edit">Edit</i></a>
                              <a class="btn　btn-danger" data-confirm="Are you sure?" dete-method="delete" href="/books/:id" rel="nofollow">Destroy</i></a>
                          </td>
                      </tr>
                  <% end %>
              </table>
          </div>
        
    </div>
</div>